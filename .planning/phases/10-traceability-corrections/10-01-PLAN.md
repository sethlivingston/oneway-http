---
plan: 10-01
phase: 10
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .planning/REQUIREMENTS.md
requirements:
  - ADR-01
  - ADR-02
  - ADR-03
  - ADR-04
  - ADR-05
  - ADR-06
  - ADR-07

must_haves:
  truths:
    - "ADR-01 through ADR-07 are checked [x] in the v1 requirements list"
    - "ADR-01 through ADR-07 show Complete (not Pending) in the traceability table"
    - "REQUIREMENTS.md footer reflects Phase 10 update date"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement and traceability state for ADR-01..07"
      contains: "- [x] **ADR-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md (lines 75-81)"
      to: ".planning/REQUIREMENTS.md (lines 168-174)"
      via: "Both sections must agree: checkboxes [x] and table status Complete"
      pattern: "ADR-0[1-7].*Complete"
---

<objective>
Mark ADR-01 through ADR-07 as complete in REQUIREMENTS.md — both the checkbox list and the traceability table.

Purpose: Phase 6 (Abort, Deadline & Retry) has passed VERIFICATION.md but the traceability docs were never updated. This blocks milestone archival.
Output: REQUIREMENTS.md with ADR-01..07 checked [x] and marked Complete in the traceability table; footer updated.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check ADR-01..07 in the v1 requirements list</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md — read lines 73–82 to confirm the exact current text of the ADR-01..07 bullet lines before editing
  </read_first>

  <action>
In .planning/REQUIREMENTS.md, locate the "### Abort, Deadline & Retry" section (lines ~75–81).

Change each of the seven unchecked requirement bullets from `[ ]` to `[x]`:

BEFORE (exact text on each line):
```
- [ ] **ADR-01**: Abort — caller-scoped `AbortSignal` passed to `send(request, { signal })`; abort surfaces as `transportError.aborted`
- [ ] **ADR-02**: Deadline — `deadlineMs` covers entire operation: all attempts + backoff sleep + body reading + decoding; expiry is terminal (no retry after deadline)
- [ ] **ADR-03**: Retry attempt count is exactly `maxAttempts - 1` retries (off-by-one prevention: condition is `attempt < maxAttempts`)
- [ ] **ADR-04**: Backoff sleep is abort-aware — deadline or caller abort during sleep surfaces immediately; no `setTimeout` that outlives the signal
- [ ] **ADR-05**: Jitter formula uses a capped range — never produces a delay exceeding `maxBackoffMs`
- [ ] **ADR-06**: Retry never triggers on `decodeError`, `unhandledStatus`, caller `aborted`, or deadline `timeout`
- [ ] **ADR-07**: Default retry policy applies to `GET`/`HEAD` methods on transport failures and 502/503/504 responses
```

AFTER (change `[ ]` → `[x]` on each of the seven lines):
```
- [x] **ADR-01**: Abort — caller-scoped `AbortSignal` passed to `send(request, { signal })`; abort surfaces as `transportError.aborted`
- [x] **ADR-02**: Deadline — `deadlineMs` covers entire operation: all attempts + backoff sleep + body reading + decoding; expiry is terminal (no retry after deadline)
- [x] **ADR-03**: Retry attempt count is exactly `maxAttempts - 1` retries (off-by-one prevention: condition is `attempt < maxAttempts`)
- [x] **ADR-04**: Backoff sleep is abort-aware — deadline or caller abort during sleep surfaces immediately; no `setTimeout` that outlives the signal
- [x] **ADR-05**: Jitter formula uses a capped range — never produces a delay exceeding `maxBackoffMs`
- [x] **ADR-06**: Retry never triggers on `decodeError`, `unhandledStatus`, caller `aborted`, or deadline `timeout`
- [x] **ADR-07**: Default retry policy applies to `GET`/`HEAD` methods on transport failures and 502/503/504 responses
```

Use seven separate Edit calls — one per ADR line — to avoid a single large replacement that could mis-match context.
  </action>

  <verify>
    <automated>grep -c '^\- \[x\] \*\*ADR-0[1-7]' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^\- \[x\] \*\*ADR-0[1-7]' .planning/REQUIREMENTS.md` returns exactly `7`
    - `grep '^\- \[ \] \*\*ADR-' .planning/REQUIREMENTS.md` returns no output (zero unchecked ADR lines)
  </acceptance_criteria>

  <done>All seven ADR requirement bullets show [x]</done>
</task>

<task type="auto">
  <name>Task 2: Update ADR-01..07 to Complete in the traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md — read lines 165–180 to confirm the exact current text of the traceability rows for ADR-01..07 before editing
  </read_first>

  <action>
In .planning/REQUIREMENTS.md, locate the "## Traceability" table (lines ~168–174).

Change each ADR row's status from `Pending` to `Complete`:

BEFORE (exact text for each of the seven rows):
```
| ADR-01 | Phase 6 | Pending |
| ADR-02 | Phase 6 | Pending |
| ADR-03 | Phase 6 | Pending |
| ADR-04 | Phase 6 | Pending |
| ADR-05 | Phase 6 | Pending |
| ADR-06 | Phase 6 | Pending |
| ADR-07 | Phase 6 | Pending |
```

AFTER (change `Pending` → `Complete` on each of the seven rows):
```
| ADR-01 | Phase 6 | Complete |
| ADR-02 | Phase 6 | Complete |
| ADR-03 | Phase 6 | Complete |
| ADR-04 | Phase 6 | Complete |
| ADR-05 | Phase 6 | Complete |
| ADR-06 | Phase 6 | Complete |
| ADR-07 | Phase 6 | Complete |
```

Then update the coverage metadata count at lines ~182–183:

BEFORE:
```
- v1 requirements: 53 total
```

AFTER:
```
- v1 requirements: 54 total
```

Then update the footer at the bottom of the file:

BEFORE:
```
*Last updated: 2026-05-11 — Phase 9: Phases 1–4 requirements marked Complete*
```

AFTER:
```
*Last updated: 2026-05-12 — Phase 10: ADR-01..07 marked Complete*
```
  </action>

  <verify>
    <automated>grep -c 'ADR-0[1-7] | Phase 6 | Complete' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '| ADR-0[1-7] | Phase 6 | Complete |' .planning/REQUIREMENTS.md` returns exactly `7`
    - `grep '| ADR-0[1-7] | Phase 6 | Pending |' .planning/REQUIREMENTS.md` returns no output (zero Pending ADR rows)
    - `grep 'v1 requirements: 54 total' .planning/REQUIREMENTS.md` matches exactly once
    - `grep 'Phase 10: ADR-01..07 marked Complete' .planning/REQUIREMENTS.md` matches exactly once
  </acceptance_criteria>

  <done>Traceability table shows Complete for all seven ADR requirements; footer updated to Phase 10</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → file | Documentation edits only; no code execution, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | REQUIREMENTS.md | accept | Documentation file only; version-controlled; no runtime impact |
</threat_model>

<verification>
After both tasks complete:

```bash
# Verify 7 checked ADR bullets
grep -c '^\- \[x\] \*\*ADR-0[1-7]' .planning/REQUIREMENTS.md
# Expected: 7

# Verify 0 unchecked ADR bullets  
grep -c '^\- \[ \] \*\*ADR-' .planning/REQUIREMENTS.md
# Expected: 0

# Verify 7 Complete rows in traceability table
grep -c '| ADR-0[1-7] | Phase 6 | Complete |' .planning/REQUIREMENTS.md
# Expected: 7

# Verify total [x] count is now 54 (was 47, +7 for ADR-01..07)
grep -c '^\- \[x\]' .planning/REQUIREMENTS.md
# Expected: 54
```
</verification>

<success_criteria>
- ADR-01..07 bullets are `[x]` (7 of 7)
- ADR-01..07 traceability rows show `Complete` (7 of 7, zero `Pending`)
- Coverage metadata updated: `v1 requirements: 54 total`
- REQUIREMENTS.md `[x]` count is 54 (was 47)
- Footer updated to Phase 10
</success_criteria>

<output>
After completion, create `.planning/phases/10-traceability-corrections/10-01-SUMMARY.md`
</output>
