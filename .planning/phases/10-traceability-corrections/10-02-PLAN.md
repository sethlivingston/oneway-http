---
plan: 10-02
phase: 10
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .planning/ROADMAP.md
requirements:
  - ADR-01
  - ADR-02
  - ADR-03
  - ADR-04
  - ADR-05
  - ADR-06
  - ADR-07

must_haves:
  truths:
    - "Phase 1, 2, 4, 6 top-level checkboxes are [x] in the Phases list"
    - "Progress table rows for Phases 1, 2, 3, 4, 6 show correct plan counts, Complete status, and completion dates"
    - "Phase 6 row shows 3/3 (not 4/4 — Phase 6 has 3 plans)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase completion status"
      contains: "- [x] **Phase 1: Infrastructure Fixes**"
  key_links:
    - from: ".planning/ROADMAP.md (lines 13-18, phase list)"
      to: ".planning/ROADMAP.md (lines ~502-507, progress table)"
      via: "Both sections must agree on completion state for each phase"
      pattern: "\\[x\\].*Phase [1246]"
---

<objective>
Update ROADMAP.md so that the top-level phase list and the progress table accurately reflect the completion state of Phases 1, 2, 3, 4, and 6.

Purpose: These phases were completed in early May 2026 but ROADMAP.md was never updated — the phase list still shows `[ ]` for Phases 1, 2, 4, and 6, and the progress table shows "0/N Not started" for all five phases. The progress table also has a wrong plan count for Phase 6 (says 4, actual is 3).
Output: ROADMAP.md with accurate checkboxes and progress table rows.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check Phase 1, 2, 4, 6 in the top-level Phases list</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md — read lines 11–26 to confirm the exact current text of the phase list bullets before editing
  </read_first>

  <action>
In .planning/ROADMAP.md, locate the "## Phases" section (lines ~11–26).

Change Phase 1, 2, 4, and 6 from `[ ]` to `[x]`. Phase 3 is already `[x]` — do not touch it.

BEFORE (exact text for the four lines):
```
- [ ] **Phase 1: Infrastructure Fixes** — Correct tsconfig, Vitest source aliases, neutral entrypoint runtime detection
- [ ] **Phase 2: Core Types + Request Model** — All shared types, `Request` class, `createClient()`, merge rules
- [ ] **Phase 4: Body Producers + Decoders** — All `Body.*` producers and `Decode.*` decoders, body normalization, `BodyPreview`
- [ ] **Phase 6: Abort, Deadline & Retry** — Retry loop, whole-operation deadline, abort-aware backoff sleep, jitter cap
```

AFTER (change `[ ]` → `[x]` on each of the four lines):
```
- [x] **Phase 1: Infrastructure Fixes** — Correct tsconfig, Vitest source aliases, neutral entrypoint runtime detection
- [x] **Phase 2: Core Types + Request Model** — All shared types, `Request` class, `createClient()`, merge rules
- [x] **Phase 4: Body Producers + Decoders** — All `Body.*` producers and `Decode.*` decoders, body normalization, `BodyPreview`
- [x] **Phase 6: Abort, Deadline & Retry** — Retry loop, whole-operation deadline, abort-aware backoff sleep, jitter cap
```

Use four separate Edit calls — one per phase line.
  </action>

  <verify>
    <automated>grep -c '^\- \[x\] \*\*Phase [1246]:' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^\- \[x\] \*\*Phase [1246]:' .planning/ROADMAP.md` returns exactly `4`
    - `grep '^\- \[ \] \*\*Phase [1246]:' .planning/ROADMAP.md` returns no output (zero unchecked among Phases 1, 2, 4, 6)
    - `grep '^\- \[x\] \*\*Phase 3:' .planning/ROADMAP.md` still matches (Phase 3 unchanged)
  </acceptance_criteria>

  <done>Phases 1, 2, 4, 6 top-level bullets show [x]; Phase 3 remains [x] unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update the progress table for Phases 1, 2, 3, 4, and 6</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md — read lines 498–516 to confirm the exact current text of the progress table before editing
  </read_first>

  <action>
In .planning/ROADMAP.md, locate the "## Progress Table" section (lines ~498–515).

Replace the five stale rows with corrected rows. Apply as five separate Edit calls — one per row.

**Row 1 — Phase 1 (3 plans, completed 2026-05-04):**
BEFORE: `| 1. Infrastructure Fixes | 0/3 | Not started | — |`
AFTER:  `| 1. Infrastructure Fixes | 3/3 | Complete    | 2026-05-04 |`

**Row 2 — Phase 2 (3 plans, completed 2026-05-05):**
BEFORE: `| 2. Core Types + Request Model | 0/3 | Not started | — |`
AFTER:  `| 2. Core Types + Request Model | 3/3 | Complete    | 2026-05-05 |`

**Row 3 — Phase 3 (3 plans, completed 2026-05-05):**
BEFORE: `| 3. Transport + Send | 0/3 | Not started | — |`
AFTER:  `| 3. Transport + Send | 3/3 | Complete    | 2026-05-05 |`

**Row 4 — Phase 4 (4 plans, completed 2026-05-06):**
BEFORE: `| 4. Body Producers + Decoders | 0/4 | Not started | — |`
AFTER:  `| 4. Body Producers + Decoders | 4/4 | Complete    | 2026-05-06 |`

**Row 5 — Phase 6 (3 plans NOT 4, completed 2026-05-07):**
BEFORE: `| 6. Abort, Deadline & Retry | 0/4 | Not started | — |`
AFTER:  `| 6. Abort, Deadline & Retry | 3/3 | Complete    | 2026-05-07 |`

Note: Phase 6 had 3 plans (06-01, 06-02, 06-03) — the old row incorrectly said 0/4.
  </action>

  <verify>
    <automated>grep -E 'Infrastructure Fixes|Core Types|Transport \+ Send|Body Producers|Abort, Deadline' .planning/ROADMAP.md | grep Complete | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep 'Infrastructure Fixes.*3/3.*Complete.*2026-05-04' .planning/ROADMAP.md` matches exactly once
    - `grep 'Core Types.*3/3.*Complete.*2026-05-05' .planning/ROADMAP.md` matches exactly once
    - `grep 'Transport + Send.*3/3.*Complete.*2026-05-05' .planning/ROADMAP.md` matches exactly once
    - `grep 'Body Producers.*4/4.*Complete.*2026-05-06' .planning/ROADMAP.md` matches exactly once
    - `grep 'Abort, Deadline.*3/3.*Complete.*2026-05-07' .planning/ROADMAP.md` matches exactly once
    - `grep 'Abort, Deadline.*0/4' .planning/ROADMAP.md` returns no output (old wrong row gone)
    - `grep -E 'Phase [1-4,6].*Not started' .planning/ROADMAP.md` returns no output (no stale Not started rows for completed phases)
  </acceptance_criteria>

  <done>Progress table rows for Phases 1, 2, 3, 4, 6 show correct plan counts, Complete status, and completion dates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → file | Documentation edits only; no code execution, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | ROADMAP.md | accept | Documentation file only; version-controlled; no runtime impact |
</threat_model>

<verification>
After both tasks complete:

```bash
# Verify 4 newly checked phases in the list (1, 2, 4, 6)
grep -c '^\- \[x\] \*\*Phase [1246]:' .planning/ROADMAP.md
# Expected: 4

# Verify Phase 3 still checked
grep '^\- \[x\] \*\*Phase 3:' .planning/ROADMAP.md
# Expected: one match

# Verify all 5 phase progress rows are Complete
grep -E 'Infrastructure Fixes|Core Types \+ Request|Transport \+ Send|Body Producers|Abort, Deadline' .planning/ROADMAP.md | grep -c Complete
# Expected: 5

# Verify Phase 6 shows 3/3 (not 4/4)
grep 'Abort, Deadline.*3/3' .planning/ROADMAP.md
# Expected: one match

# Verify no stale 0/N Not started rows for completed phases
grep 'Not started' .planning/ROADMAP.md | grep -vE 'Phase (10|11|12|13)'
# Expected: no output
```
</verification>

<success_criteria>
- Phase 1, 2, 4, 6 top-level bullets are `[x]` (4 of 4 changed)
- Phase 3 bullet remains `[x]` (untouched)
- Progress table: Phases 1, 2, 3, 4, 6 all show `Complete` with correct plan counts and dates
- Phase 6 plan count corrected from 4 to 3
</success_criteria>

<output>
After completion, create `.planning/phases/10-traceability-corrections/10-02-SUMMARY.md`
</output>
