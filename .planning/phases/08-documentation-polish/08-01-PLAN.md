---
phase: 08-documentation-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/send.ts
  - src/index.ts
  - tests/unit/types.test.ts
  - tests/unit/send.test.ts
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "RequestError union has six variants including reservedResponseTag"
    - "performSend() returns requestError.reservedResponseTag when spec.responses contains a reserved tag"
    - "performSend() returns requestError.reservedResponseTag when clientSpec.responses contains a reserved tag"
    - "runtimeTarget and describe are not exported from src/index.ts"
    - "createClient, Client, and Request are exported from src/index.ts"
    - "All exported types in types.ts have /** Brief description. */ TSDoc comments"
  artifacts:
    - path: "src/types.ts"
      provides: "RequestError union with 6 variants + TSDoc on all exports"
      contains: "reservedResponseTag"
    - path: "src/send.ts"
      provides: "RESERVED_RESPONSE_TAGS const + validation loops"
      contains: "RESERVED_RESPONSE_TAGS"
    - path: "src/index.ts"
      provides: "Clean public API surface — no scaffolding exports"
      contains: "createClient"
    - path: "tests/unit/types.test.ts"
      provides: "Updated TYPES-09 test for 6 variants"
      contains: "six variants"
    - path: "tests/unit/send.test.ts"
      provides: "Reserved tag validation test coverage"
      contains: "reservedResponseTag"
  key_links:
    - from: "src/types.ts"
      to: "src/send.ts"
      via: "RequestError.reservedResponseTag imported in satisfies assertion"
      pattern: "reservedResponseTag"
    - from: "src/send.ts"
      to: "RESERVED_RESPONSE_TAGS"
      via: "module-level Set alongside DECODE_ERROR_KINDS"
      pattern: "RESERVED_RESPONSE_TAGS"
---

<objective>
Deliver the three code changes required before documentation can be written:
1. Add `reservedResponseTag` to `RequestError` and wire runtime validation in `performSend()`.
2. Remove scaffolding exports (`runtimeTarget`, `describe`) from `src/index.ts`; add missing public API exports (`createClient`, `Client`, `Request`, full type surface).
3. Add TSDoc to every exported type/interface/alias in `src/types.ts`.
4. Update tests to cover the new variant and validate the runtime guard.

Purpose: Correct public API surface and type-level completeness are prerequisites for accurate README and TSDoc in subsequent plans.
Output: src/types.ts (6-variant RequestError + TSDoc), src/send.ts (validation guard), src/index.ts (clean exports), updated unit tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-documentation-polish/08-CONTEXT.md
@.planning/phases/08-documentation-polish/08-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/types.ts (current RequestError — 5 variants):
```typescript
export type RequestError =
  | { kind: "bodySerializationFailed"; message: string }
  | { kind: "requestConsumed" }
  | { kind: "missingBaseUrl" }
  | { kind: "duplicateResponseTag"; tag: string }
  | { kind: "invalidSpec"; message: string };
```
New variant to add: `| { kind: "reservedResponseTag"; tag: string }`

From src/send.ts (module-level consts, ~line 87):
```typescript
const DECODE_ERROR_KINDS = new Set([
  "unexpectedBody", "emptyBody", "invalidJson",
  "schemaMismatch", "bodyReadFailed", "custom",
]);
```
Add RESERVED_RESPONSE_TAGS alongside this const.

From src/send.ts (performSend body, placement point ~line 154):
The combinedSignal assignment ends at line ~154. The body serialization block begins at ~line 157.
Insert reserved tag validation loops between these two blocks.

From src/index.ts (current — scaffolding to remove):
```typescript
import { createPlaceholderSurface, type OnewayHttpSurface, type RuntimeTarget } from "./shared.js";
const rootSurface = createPlaceholderSurface(...);
export const runtimeTarget: RuntimeTarget = rootSurface.runtimeTarget;
export const describe: OnewayHttpSurface["describe"] = rootSurface.describe;
```
Replace entire file — remove the shared.js import block; add createClient, Client, Request; expand type exports.

From src/matcher.ts (ReservedTags — compile-time only, NOT exported):
```typescript
type ReservedTags = "transportError" | "decodeError" | "unhandledStatus" | "requestError";
```
The RESERVED_RESPONSE_TAGS Set must list the same four strings. Cannot import from matcher.ts — dependency direction violation.

From tests/unit/types.test.ts (TYPES-09 block to update):
```typescript
describe("TYPES-09: RequestError union — five variants", () => {
  it("all five variants are assignable", () => {
    const variants: RequestError[] = [
      { kind: "bodySerializationFailed", message: "circular ref" },
      { kind: "requestConsumed" },
      { kind: "missingBaseUrl" },
      { kind: "duplicateResponseTag", tag: "ok" },
      { kind: "invalidSpec", message: "bad spec" },
    ];
    expect(variants).toHaveLength(5);
  });
});
```
Update: "six variants", add `{ kind: "reservedResponseTag", tag: "transportError" }`, `toHaveLength(6)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reservedResponseTag variant and TSDoc to src/types.ts</name>
  <files>src/types.ts</files>
  <behavior>
    - `{ kind: "reservedResponseTag", tag: "transportError" }` is assignable to `RequestError`
    - `{ kind: "reservedResponseTag", tag: "foo" }` is assignable to `RequestError`
    - The variant has both `kind` and `tag` fields
    - TSDoc `/** ... */` comment is present on every exported declaration
  </behavior>
  <read_first>
    - src/types.ts (read current state before editing)
    - .planning/phases/08-documentation-polish/08-CONTEXT.md (D-01: TSDoc format rules)
  </read_first>
  <action>
Add `| { kind: "reservedResponseTag"; tag: string }` as the sixth and final variant of `RequestError` (after the `invalidSpec` variant).

Then add a `/** Brief description. */` TSDoc comment to every exported declaration in `src/types.ts`. The exact TSDoc for each export (per D-01 — no @example blocks):

```typescript
/** HTTP method string. Includes `"QUERY"` (IESG-approved, pending RFC). */
export type Method = ...

/** Scalar value accepted as a URL query parameter. */
export type QueryValue = string | number | boolean;

/** Matches an HTTP status by exact code or by class (`"2xx"`, `"4xx"`, `"5xx"`). */
export type StatusMatcher = number | "2xx" | "4xx" | "5xx";

/** Duck-type interface for Zod-compatible schema validators. */
export interface Schema<T> { ... }

/** Opaque body token produced by `Body.*` factory methods. */
export declare class Body { ... }

/** A single structured issue from a schema validation failure. */
export interface DecodeIssue { ... }

/** Structured error describing why a response body could not be decoded. */
export type DecodeError = ...

/** First N bytes of a response body captured for diagnostic display. */
export interface BodyPreview { ... }

/** Structured error classifying a transport-layer failure. */
export type TransportError = ...

/** Structured error classifying a request configuration problem. */
export type RequestError = ...

/** Discriminated union representing every possible outcome of `client.send()`. */
export type SendResult<R> = ...

/** Minimal interface for a response decoder accepted by `TaggedEntry`. */
export interface DecoderLike { ... }

/** Pairs a decoder with a tag string for use in a `ResponseMap`. */
export interface TaggedEntry<T = unknown, Tag extends string = string> { ... }

/** Maps status matchers to tagged decoder entries. */
export type ResponseMap = Partial<Record<StatusMatcher, TaggedEntry>>;

/** Extracts the response union type from a `ResponseMap`. */
export type InferResponseUnion<M extends ResponseMap> = ...

/** Per-request retry tuning options. */
export interface RetryOptions { ... }

/** Retry policy: `true` for defaults, `false` to disable, or `RetryOptions` for custom config. */
export type RetryPolicy = true | false | RetryOptions;

/** Shared request fields used by both `RequestSpec` variants. */
export interface RequestSpecBase<Responses extends ResponseMap = ResponseMap> { ... }

/** Request specification: either a `path` (resolved against `baseUrl`) or an `absoluteUrl`. */
export type RequestSpec<Responses extends ResponseMap = ResponseMap> = ...

/** Per-call options passed to `client.send()`. */
export interface SendOptions { signal?: AbortSignal; }

/** Configuration for `createClient()`. */
export interface ClientSpec { ... }
```

Rules (per D-01):
- One-line `/** Brief description. */` format for types and interfaces
- No `@example` blocks in TSDoc
- The file MUST retain zero imports — adding TSDoc requires no new imports
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `src/types.ts` has `| { kind: "reservedResponseTag"; tag: string }` in the RequestError union
    - Every exported declaration has a `/**` TSDoc comment immediately above it
    - `grep -c 'reservedResponseTag' src/types.ts` returns at least 1
    - `grep -c '/\*\*' src/types.ts` returns at least 20 (one per exported symbol)
    - `grep '^\s*import' src/types.ts | wc -l` returns 0 (zero imports invariant intact)
    - `npx tsc --noEmit` passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RESERVED_RESPONSE_TAGS const and validation loops to src/send.ts</name>
  <files>src/send.ts</files>
  <behavior>
    - performSend() returns `{ kind: "requestError", error: { kind: "reservedResponseTag", tag: "transportError" } }` when spec.responses has an entry with tag "transportError"
    - Same result for tags "decodeError", "unhandledStatus", "requestError"
    - performSend() returns `{ kind: "requestError", error: { kind: "reservedResponseTag", tag: "transportError" } }` when clientSpec.responses has the reserved tag
    - A non-reserved tag (e.g., "ok") does NOT trigger the guard
    - RESERVED_RESPONSE_TAGS is defined at module level, not inside performSend
  </behavior>
  <read_first>
    - src/send.ts (read full file — 304 lines — before editing)
    - src/types.ts (confirm reservedResponseTag variant added in Task 1)
    - .planning/phases/08-documentation-polish/08-RESEARCH.md (exact placement and loop pattern)
  </read_first>
  <action>
**Step 1 — Add module-level const** (insert immediately after the closing `}` of `isDecodeError()`, around line 100, alongside `DECODE_ERROR_KINDS`):

```typescript
// Runtime guard for reserved tag names — complements compile-time Exclude<TagsOf<R>, ReservedTags>.
// Defined at module scope to avoid per-call allocation.
// Must stay in sync with ReservedTags in src/matcher.ts.
// Cannot import ReservedTags from matcher.ts — violates dependency direction (send.ts → types.ts only).
const RESERVED_RESPONSE_TAGS = new Set([
  "transportError",
  "decodeError",
  "unhandledStatus",
  "requestError",
]);
```

**Step 2 — Add validation loops inside `performSend()`** (insert after the `combinedSignal` assignment block, before the body serialization `try` block — between lines ~154 and ~157 in the original file):

```typescript
  // Reserved tag guard: reject any response map entry whose tag collides with a SendResult discriminant.
  // spec.responses check:
  for (const entry of Object.values(spec.responses ?? {})) {
    if (entry !== undefined && RESERVED_RESPONSE_TAGS.has(entry.tag)) {
      clearTimeout(deadlineTimer); // safe — clearTimeout(undefined) is a no-op
      return {
        kind: "requestError",
        error: { kind: "reservedResponseTag", tag: entry.tag } satisfies RequestError,
      };
    }
  }
  // clientSpec.responses check:
  for (const entry of Object.values(clientSpec.responses ?? {})) {
    if (entry !== undefined && RESERVED_RESPONSE_TAGS.has(entry.tag)) {
      clearTimeout(deadlineTimer);
      return {
        kind: "requestError",
        error: { kind: "reservedResponseTag", tag: entry.tag } satisfies RequestError,
      };
    }
  }
```

Note: `spec.responses` is `Responses` (required field on `RequestSpecBase`). It may be an empty object `{}` — `Object.values({})` produces zero iterations, no problem. `clientSpec.responses` is optional (`ResponseMap | undefined`) — `?? {}` handles undefined.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/send.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c 'RESERVED_RESPONSE_TAGS' src/send.ts` returns at least 3 (declaration + two `.has()` calls)
    - `grep 'reservedResponseTag' src/send.ts` shows the satisfies assertion
    - The const is at module scope (outside any function) — verify by checking it appears before `export async function performSend`
    - `npx tsc --noEmit` passes
    - Existing send.test.ts tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Clean src/index.ts exports + update unit tests</name>
  <files>src/index.ts, tests/unit/types.test.ts, tests/unit/send.test.ts</files>
  <behavior>
    - `import { createClient } from "@sethlivingston/oneway-http"` resolves (createClient is exported)
    - `import { Request } from "@sethlivingston/oneway-http"` resolves (Request is exported)
    - `runtimeTarget` is NOT exported from src/index.ts
    - `describe` is NOT exported from src/index.ts
    - TYPES-09 test describes "six variants" and includes reservedResponseTag
    - SEND-XX test covers all four reserved tag strings and both spec.responses + clientSpec.responses paths
  </behavior>
  <read_first>
    - src/index.ts (read current state before replacing)
    - tests/unit/types.test.ts (find TYPES-09 describe block to update)
    - tests/unit/send.test.ts (read to find last describe block number + existing patterns)
    - src/client.ts (confirm exported names: createClient, Client, mergeHeaders, mergeQuery)
    - src/request.ts (confirm exported names: Request, buildPath, buildQuery)
  </read_first>
  <action>
**A. Replace src/index.ts entirely** with the following content (removes shared.js scaffolding; adds all public API exports; expands type surface):

```typescript
// src/index.ts — public API surface for @sethlivingston/oneway-http
// Scaffolding exports (runtimeTarget, describe) removed per D-06.

// Body value type and producers (Body.none(), Body.json(), Body.text(), Body.bytes(), Body.formUrlEncoded())
export { Body } from "./body.js";

// Decoder class and Decode namespace producers (Decode.none(), Decode.json(), Decode.text(), etc.)
export { Decoder, Decode } from "./decode.js";

// Request class — use Request.create() to build a typed request
export { Request } from "./request.js";

// Client factory — use createClient(spec) to build a Client instance
export { createClient } from "./client.js";
export type { Client } from "./client.js";

// Send.match() dispatcher and Matcher<R,T> exhaustive handler type
export { Send } from "./matcher.js";
export type { Matcher } from "./matcher.js";

// All public types
export type {
  Method,
  QueryValue,
  StatusMatcher,
  Schema,
  DecodeIssue,
  DecodeError,
  BodyPreview,
  TransportError,
  RequestError,
  SendResult,
  DecoderLike,
  TaggedEntry,
  ResponseMap,
  InferResponseUnion,
  RetryOptions,
  RetryPolicy,
  RequestSpecBase,
  RequestSpec,
  SendOptions,
  ClientSpec,
} from "./types.js";
```

**B. Update tests/unit/types.test.ts — TYPES-09 block only:**

Change the describe string from `"TYPES-09: RequestError union — five variants"` to `"TYPES-09: RequestError union — six variants"`.

Change the it string from `"all five variants are assignable"` to `"all six variants are assignable"`.

Add `{ kind: "reservedResponseTag", tag: "transportError" }` as the sixth element in the `variants` array.

Change `expect(variants).toHaveLength(5)` to `expect(variants).toHaveLength(6)`.

**C. Add new describe block to tests/unit/send.test.ts** (append at end of file):

```typescript
describe("SEND-NN: reserved tag validation (D-07, D-08)", () => {
  const mockFetch: typeof globalThis.fetch = async () => new Response(null, { status: 200 });
  const stubDecode = { fn: async (_r: Response): Promise<unknown> => undefined };

  it.each(["transportError", "decodeError", "unhandledStatus", "requestError"] as const)(
    "returns requestError.reservedResponseTag for reserved tag '%s' in spec.responses",
    async (reservedTag) => {
      const req = Request.create({
        method: "GET",
        path: [],
        responses: {
          200: { tag: reservedTag, decode: stubDecode },
        },
      });
      const result = await performSend(req, {
        baseUrl: "https://api.example.com/",
        fetch: mockFetch,
      });
      expect(result.kind).toBe("requestError");
      if (result.kind === "requestError") {
        expect(result.error.kind).toBe("reservedResponseTag");
        if (result.error.kind === "reservedResponseTag") {
          expect(result.error.tag).toBe(reservedTag);
        }
      }
    },
  );

  it("returns requestError.reservedResponseTag for reserved tag in clientSpec.responses", async () => {
    const req = Request.create({ method: "GET", path: [], responses: {} });
    const result = await performSend(req, {
      baseUrl: "https://api.example.com/",
      fetch: mockFetch,
      responses: {
        200: { tag: "transportError", decode: stubDecode },
      },
    });
    expect(result.kind).toBe("requestError");
    if (result.kind === "requestError") {
      expect(result.error.kind).toBe("reservedResponseTag");
    }
  });

  it("does NOT trigger for a non-reserved tag in spec.responses", async () => {
    const req = Request.create({
      method: "GET",
      path: [],
      responses: {
        200: { tag: "ok", decode: stubDecode },
      },
    });
    const result = await performSend(req, {
      baseUrl: "https://api.example.com/",
      fetch: mockFetch,
    });
    expect(result.kind).not.toBe("requestError");
  });
});
```

Note: `SEND-NN` — replace `NN` with the next sequential number after the last existing `SEND-XX` describe block number found in the file.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/types.test.ts tests/unit/send.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `grep -c 'runtimeTarget\|from.*shared' src/index.ts | grep -v '^0'` returns nothing (no such lines)
    - `grep 'createClient' src/index.ts` shows the export line
    - `grep 'Request.*from.*request' src/index.ts` shows the export line
    - `grep 'six variants' tests/unit/types.test.ts` returns the updated describe string
    - `grep 'reservedResponseTag' tests/unit/types.test.ts` returns the new variant entry
    - `grep 'reservedResponseTag' tests/unit/send.test.ts` returns the test case
    - `npx vitest run tests/unit/types.test.ts tests/unit/send.test.ts` passes all tests
    - `npx tsc --noEmit` passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → performSend() | Caller supplies `RequestSpec.responses` and `ClientSpec.responses`; these are untrusted tag strings at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | performSend() response tag validation | mitigate | RESERVED_RESPONSE_TAGS guard in performSend() returns requestError.reservedResponseTag before fetch; prevents caller from injecting a tag that shadows a SendResult discriminant |
| T-08-02 | Information Disclosure | src/index.ts scaffolding exports | mitigate | Remove runtimeTarget and describe exports per D-06; these expose internal implementation shape with no consumer value |
| T-08-03 | Spoofing | RequestError variant collision | accept | New reservedResponseTag variant is structurally distinct (has `tag` field); no variant confusion risk within the union |
</threat_model>

<verification>
```bash
# Type-check entire project
npx tsc --noEmit

# All unit tests pass
npx vitest run tests/unit/

# No runtimeTarget or describe exported from index.ts
grep -v '^//' src/index.ts | grep -c 'runtimeTarget\|export.*describe'
# Must return 0

# reservedResponseTag present in types.ts and send.ts
grep -c 'reservedResponseTag' src/types.ts src/send.ts
# Must return >= 1 per file

# RESERVED_RESPONSE_TAGS at module scope in send.ts
grep -n 'RESERVED_RESPONSE_TAGS' src/send.ts | head -5

# Zero imports invariant on types.ts
grep -v '^//' src/types.ts | grep -c '^\s*import'
# Must return 0
```
</verification>

<success_criteria>
- `RequestError` union has exactly 6 variants; `{ kind: "reservedResponseTag", tag: string }` is one of them
- `performSend()` returns `{ kind: "requestError", error: { kind: "reservedResponseTag", tag } }` for all four reserved tag strings in both spec.responses and clientSpec.responses
- `src/index.ts` exports `createClient`, `Client`, `Request`, and all 20 type names from types.ts; does NOT export `runtimeTarget` or `describe`
- All TSDoc comments in `src/types.ts` follow `/** Brief description. */` format with no `@example` blocks
- `npx tsc --noEmit` passes; all unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-polish/08-01-SUMMARY.md`
</output>
