---
phase: 01-infrastructure-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
files_modified:
  - src/index.ts
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "The neutral entrypoint exports runtimeTarget: \"node\" when imported in Node.js"
    - "The neutral entrypoint exports runtimeTarget: \"browser\" when imported in a browser context"
    - "Runtime detection uses typeof globalThis.document (not window) — document is browser-only"
    - "All four parity projects (node, chromium, firefox, webkit) pass with no regressions"
    - "src/index.ts has no hardcoded string literal \"browser\" in the createPlaceholderSurface() call"
  artifacts:
    - path: src/index.ts
      provides: "Runtime-detected runtimeTarget via globalThis.document check"
      contains: "typeof globalThis.document"
  key_links:
    - from: src/index.ts
      to: createPlaceholderSurface()
      via: "runtime ternary passed as argument"
      pattern: "typeof globalThis\\.document !== \"undefined\""
    - from: src/index.ts
      to: "exported runtimeTarget"
      via: "rootSurface.runtimeTarget assignment"
      pattern: "runtimeTarget: RuntimeTarget = rootSurface"
---

<objective>
Fix `src/index.ts` line 7 to detect the runtime at runtime instead of hardcoding `"browser"`. Replace the literal `"browser"` argument in `createPlaceholderSurface("browser")` with a ternary that checks `typeof globalThis.document !== "undefined"`.

Purpose: The neutral entrypoint is imported in both Node.js and browser environments through the package root export. A hardcoded `"browser"` causes the Node parity test to see `runtimeTarget: "browser"` and fail its `__ONEWAY_HTTP_EXPECTED_ROOT_TARGET__` assertion. The `document` check is correct for all relevant runtimes — Deno, Bun, Cloudflare Workers, and Node all lack `document`.

Output: Updated `src/index.ts` with runtime detection; parity test suite passes in all four projects.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-fixes/01-CONTEXT.md
@.planning/phases/01-infrastructure-fixes/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-fixes/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded "browser" with runtime detection in src/index.ts (D-10, D-11)</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts — read the current full content (10 lines); note line 7 is the createPlaceholderSurface() call
    - src/browser.ts — sibling entrypoint showing the same file shape with hardcoded "browser"
    - src/node.ts — sibling entrypoint showing the same file shape with hardcoded "node"
    - .planning/phases/01-infrastructure-fixes/01-CONTEXT.md — D-10 and D-11 specify exact expression and rationale
    - .planning/phases/01-infrastructure-fixes/01-PATTERNS.md — contains exact target state of the file
  </read_first>
  <action>
Replace line 7 of `src/index.ts` only. The import block (lines 1–5) and the two export lines (lines 9–10) are unchanged.

**Current line 7:**
```typescript
const rootSurface = createPlaceholderSurface("browser");
```

**Replacement line 7 (D-10, D-11):**
```typescript
const rootSurface = createPlaceholderSurface(
  typeof globalThis.document !== "undefined" ? "browser" : "node",
);
```

The complete target file content:
```typescript
import {
  createPlaceholderSurface,
  type OnewayHttpSurface,
  type RuntimeTarget,
} from "./shared.js";

const rootSurface = createPlaceholderSurface(
  typeof globalThis.document !== "undefined" ? "browser" : "node",
);

export const runtimeTarget: RuntimeTarget = rootSurface.runtimeTarget;
export const describe: OnewayHttpSurface["describe"] = rootSurface.describe;
```

Implementation notes:
- Use `globalThis.document` (not `window.document` or bare `document`) — `globalThis` is always defined, avoiding ReferenceError in strict ESM environments (D-11)
- Check `!== "undefined"` (string), not `!= null` — `typeof` always returns a string
- `RuntimeTarget` type import stays — still needed for the explicit annotation on the `runtimeTarget` export
- Local variable name stays `rootSurface` — consistent with existing convention
- Both sibling entrypoints (`src/browser.ts`, `src/node.ts`) remain unchanged — they hardcode their targets by design
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run --project node 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/index.ts contains `typeof globalThis.document !== "undefined"` (grep: `grep -c 'typeof globalThis.document' src/index.ts` returns 1)
    - src/index.ts does NOT contain `createPlaceholderSurface("browser")` as a simple literal call (grep: `grep -c 'createPlaceholderSurface("browser")' src/index.ts` returns 0)
    - src/index.ts still exports `runtimeTarget` with `RuntimeTarget` type annotation (grep: `grep -c 'runtimeTarget: RuntimeTarget' src/index.ts` returns 1)
    - `npm run typecheck` exits 0
    - `npx vitest run --project node` exits 0 (node parity test sees `runtimeTarget: "node"`)
  </acceptance_criteria>
  <done>src/index.ts uses runtime detection; tsc passes; node parity test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Run full parity test suite across all four projects</name>
  <files></files>
  <read_first>
    - vitest.config.ts — confirm resolve.alias is present (from Plan 02); this is required for tests to run without a build
    - .planning/phases/01-infrastructure-fixes/01-02-SUMMARY.md — confirm Plan 02 aliases are in place
  </read_first>
  <action>
Run the full parity test suite across all four projects (node, chromium, firefox, webkit) to confirm:
1. `runtimeTarget: "node"` in the Node project
2. `runtimeTarget: "browser"` in the three browser projects
3. No regressions in any project

Command:
```bash
npx vitest run
```

This runs all four projects. The `__ONEWAY_HTTP_EXPECTED_ROOT_TARGET__` define in each vitest project config determines what value each test asserts:
- Node project: expects `"node"` (defined as `JSON.stringify("node")`)
- Browser projects (chromium/firefox/webkit): expect `"browser"` (defined as `JSON.stringify("browser")`)

If any project fails, read the error output carefully:
- A node project failure with `runtimeTarget: "browser"` means the ternary isn't evaluating correctly — double-check that `typeof globalThis.document` check is correct
- A browser project failure with `runtimeTarget: "node"` means `globalThis.document` is unexpectedly undefined in the browser test environment — check Vite/Playwright browser environment setup
- Any import resolution error means the vitest aliases from Plan 02 are not in place — confirm `resolve.alias` is present in `vitest.config.ts`
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - All four projects (node, chromium, firefox, webkit) report passing
    - Zero test failures across all projects
    - No import resolution errors (which would indicate Plan 02 aliases are missing)
  </acceptance_criteria>
  <done>All four parity projects pass; no regressions; runtimeTarget detection works in both Node and browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| globalThis.document check → runtime classification | Environment fingerprinting used to set informational metadata |
| src/index.ts → consumers via package root export | Consumers using the neutral entrypoint receive a runtime-detected target value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | `globalThis.document` spoofing in Node environments with DOM polyfills (e.g., jsdom, happy-dom) | accept | Per D-12: `runtimeTarget` is purely informational metadata — no behavioral gating on this value. Nothing in the library changes behavior based on `runtimeTarget`. Consumers needing precise runtime targeting should use explicit subpath imports (`/browser` or `/node`) per D-13. |
| T-03-02 | Information Disclosure | `window` check (rejected) would classify Deno, Bun, and Cloudflare Workers as browsers | accept | D-11 mandates `document` check (not `window`) — `document` is absent in all non-browser environments listed. `window` is present in some non-browser environments. This decision is locked. |
| T-03-03 | Tampering | Import of `createPlaceholderSurface` from `./shared.js` unchanged | accept | The `.js` extension is preserved on the import (not `.ts`). Post-tsconfig migration, TypeScript's `Bundler` resolution accepts `.ts` without extension, but the `.js` form is correct for the published ESM dist and must not be changed. |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. Runtime detection expression present
grep 'typeof globalThis.document' src/index.ts

# 2. Hardcoded literal gone
grep -c 'createPlaceholderSurface("browser")' src/index.ts  # must output 0

# 3. Typecheck passes
npm run typecheck

# 4. Full parity suite passes
npx vitest run
```
</verification>

<success_criteria>
1. `src/index.ts` line 7 contains `typeof globalThis.document !== "undefined" ? "browser" : "node"`.
2. `npm run typecheck` exits 0.
3. `npx vitest run` exits 0 with all four projects (node, chromium, firefox, webkit) passing.
4. Node project asserts `runtimeTarget === "node"`; browser projects assert `runtimeTarget === "browser"`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-fixes/01-03-SUMMARY.md`
</output>
