---
phase: 09-retroactive-phase-verifications
plan: 04
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
files_modified:
  - .planning/phases/04-body-producers-decoders/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BODY-01
  - BODY-02
  - BODY-03
  - BODY-04
  - BODY-05
  - DEC-01
  - DEC-02
  - DEC-03
  - DEC-04
  - DEC-05
  - DEC-06
  - DEC-07
  - DEC-08

must_haves:
  truths:
    - "04-VERIFICATION.md exists with frontmatter `status: passed`"
    - "All 13 Phase 4 requirements (BODY-01..05, DEC-01..08) covered with evidence"
    - "DEC-01 documented: none() peek-one-chunk strategy, unexpectedBody on non-empty"
    - "DEC-05 documented: normalizeSchemaError duck-typing (no ZodError instanceof)"
    - "DEC-08 documented: null body + empty stream both normalize to zero bytes"
    - "Phase 4's addition of requestError (5th SendResult variant) noted"
    - "REQUIREMENTS.md updated: all 34 Phase 1-4 requirement checkboxes changed from [ ] to [x]"
    - "REQUIREMENTS.md traceability table: all 34 Phase 1-4 rows changed from Pending to Complete"
  artifacts:
    - path: ".planning/phases/04-body-producers-decoders/04-VERIFICATION.md"
      provides: "Formal acceptance record for Phase 4"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement status"
      contains: "[x] **INFRA-01**"
  key_links:
    - from: "REQUIREMENTS.md"
      to: "All 34 Phase 1-4 requirements"
      via: "Checkbox update [ ] → [x] and traceability table Pending → Complete"
      pattern: "\\[x\\].*INFRA\\|\\[x\\].*TYPES\\|\\[x\\].*REQ-\\|\\[x\\].*SEND\\|\\[x\\].*BODY\\|\\[x\\].*DEC-"
---

<objective>
Write the retroactive VERIFICATION.md for Phase 4: Body Producers + Decoders, then update REQUIREMENTS.md to mark all 34 Phase 1–4 requirements as Complete.

Task 1 closes the final 13 Phase 4 documentation gaps (BODY-01..05, DEC-01..08).
Task 2 changes all 34 `[ ]` checkboxes to `[x]` in REQUIREMENTS.md and updates the traceability table from Pending → Complete — satisfying the Phase 9 success criteria.

Purpose: After Task 2, the milestone audit should score ≥50/53 requirements.
Output: 04-VERIFICATION.md + updated REQUIREMENTS.md.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-body-producers-decoders/04-01-SUMMARY.md
@.planning/phases/04-body-producers-decoders/04-02-SUMMARY.md
@.planning/phases/04-body-producers-decoders/04-03-SUMMARY.md
@.planning/phases/04-body-producers-decoders/04-04-SUMMARY.md
@.planning/phases/06-abort-deadline-retry/06-VERIFICATION.md

<interfaces>
<!-- Current codebase state — verified before writing plan -->

src/body.ts:
  - `export const Body = { none(), json(value), text(value, contentType?), formUrlEncoded(entries), bytes(bytes, contentType?) }`
  - `export function serializeBody(body)` — NOT re-exported from index.ts (D-06)
  - Opaque cast via toBody()/fromBody() using `as unknown as T`
  - TS6 fix: `bytes` typed as `Uint8Array<ArrayBuffer>` (BufferSource = ArrayViewBuffer<ArrayBuffer> in TS6)

src/decode.ts:
  - `export class Decoder<T>` — wraps fn, exposes `.as<Tag>(tag)` for tagged entries
  - `export async function readBytes(response)` — null-body guard (returns new Uint8Array(0) if body===null) + for...of assembly
  - `export const Decode = { none(), discard(), text(), json(), json(schema), bytes(), optional(inner) }`
  - `normalizeSchemaError()` — duck-types `.issues` array; NEVER instanceof ZodError; handles non-Zod errors too
  - DEC-01 none(): reads one chunk via reader.read(); done:true → void; done:false → cancel + unexpectedBody
  - DEC-02 discard(): `response.body?.cancel().catch(() => {})` — no reader allocation

src/preview.ts:
  - `export async function readBodyPreview(response, maxBytes)` — extracted from send.ts in Phase 4 Plan 04-03
  - Peek-read truncation: reads one extra chunk when bytesRead === maxBytes to detect "exactly full" vs "more data"
  - TextDecoder('utf-8', { fatal: false }) with outer try/catch — decode errors return text: ""
  - reader.cancel() in finally — TCP socket release

src/types.ts additions from Phase 4:
  - `export declare class Body` — opaque type token
  - `export type RequestError` — 5-variant union (bodySerializationFailed, requestConsumed, missingBaseUrl, duplicateResponseTag, invalidSpec)
  - SendResult<R> 5th variant: `{ kind: "requestError"; error: RequestError }`

REQUIREMENTS.md current state (34 rows showing "Pending" in traceability table):
  Phase 1: INFRA-01, INFRA-02, INFRA-03 (lines ~127-131)
  Phase 2: TYPES-01..08, REQ-01..04 (lines ~132-145)
  Phase 3: SEND-01..06 (lines ~146-157)
  Phase 4: BODY-01..05, DEC-01..08 (lines ~158-178)
  All 34 show "Pending" in the Status column — must be changed to "Complete"
  Checkboxes in requirements list also show [ ] — must be changed to [x]

Test state: 180 passed | 3 skipped | 1 todo (184), exit 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 04-VERIFICATION.md</name>
  <files>.planning/phases/04-body-producers-decoders/04-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/04-body-producers-decoders/04-01-SUMMARY.md (body.ts, types.ts RequestError + 5th SendResult variant, Body opaque type, TS6 Uint8Array fix)
    - .planning/phases/04-body-producers-decoders/04-02-SUMMARY.md (decode.ts: Decoder class, readBytes, Decode namespace, normalizeSchemaError, DEC-01 peek strategy, DEC-02 discard via cancel)
    - .planning/phases/04-body-producers-decoders/04-03-SUMMARY.md (preview.ts extracted, send.ts wired for body serialization, requestError return)
    - .planning/phases/04-body-producers-decoders/04-04-SUMMARY.md (index.ts exports added, Body type export collision workaround)
    - .planning/phases/06-abort-deadline-retry/06-VERIFICATION.md (canonical VERIFICATION.md format)
    - src/body.ts (confirm Body namespace shape, serializeBody export absence from index.ts)
    - src/decode.ts (confirm Decoder class, Decode namespace, normalizeSchemaError duck-typing, readBytes null-body guard)
    - src/preview.ts (confirm readBodyPreview, peek-read, TextDecoder, finally cancel)
  </read_first>

  <action>
Create `.planning/phases/04-body-producers-decoders/04-VERIFICATION.md` with the following exact structure and content:

**FRONTMATTER** (use today's date as ISO 8601):
```yaml
---
phase: 04-body-producers-decoders
verified: [TODAY_ISO_DATE]
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
---
```

**HEADER BLOCK:**
```markdown
# Phase 4: Body Producers + Decoders — Verification Report

**Phase Goal:** Implement the Body producer namespace (Body.none/json/text/formUrlEncoded/bytes), the Decoder class and Decode decoder namespace (Decode.none/discard/text/json/bytes/optional), extract readBodyPreview into src/preview.ts, wire body serialization into performSend(), and add the requestError variant to SendResult<R>.
**Verified:** [TODAY_ISO_DATE]
**Status:** ✅ PASSED
**Re-verification:** No — initial verification
```

**GOAL ACHIEVEMENT section:**

Observable Truths table — 8 rows:
1. `Body.none/json/text/formUrlEncoded/bytes all produce opaque Body tokens` | ✓ VERIFIED | `grep 'export const Body' src/body.ts` → present; 5 factory methods confirmed; `grep 'toBody' src/body.ts` → opaque cast via `as unknown as BodyOpaque`
2. `Decode namespace exports none/discard/text/json/bytes/optional — all return Decoder<T> instances` | ✓ VERIFIED | `grep 'export const Decode' src/decode.ts` → present; `grep 'export class Decoder' src/decode.ts` → present; all 6 factories present
3. `Decode.none() returns decodeError.unexpectedBody for any non-empty body` | ✓ VERIFIED | `grep 'unexpectedBody' src/decode.ts` → present in none() implementation; DEC-01 tests (peek-one-chunk strategy) pass
4. `Decode.json(schema) normalizes validation errors without instanceof ZodError` | ✓ VERIFIED | `grep 'normalizeSchemaError' src/decode.ts` → present; duck-types `.issues` array; `grep 'ZodError\|instanceof' src/decode.ts` → 0 (no Zod-specific code). DEC-05 tests pass.
5. `readBytes() normalizes null body and empty stream to zero bytes (DEC-08)` | ✓ VERIFIED | `grep 'body.*null\|null.*body' src/decode.ts` → null-body guard returns `new Uint8Array(0)`; DEC-08 tests pass
6. `src/preview.ts exports readBodyPreview (extracted from send.ts)` | ✓ VERIFIED | `grep 'export async function readBodyPreview' src/preview.ts` → present; `grep 'async function readBodyPreview' src/send.ts` → 0 (removed from send.ts); peek-read truncation detection present in preview.ts
7. `Body is exported from index.ts; serializeBody is NOT exported` | ✓ VERIFIED | `grep 'export { Body }' src/index.ts` → present; `grep 'serializeBody' src/index.ts` → 0
8. `npm run verify exits 0 with 180 tests passing` | ✓ VERIFIED | `Test Files 13 passed (13) / Tests 180 passed | 3 skipped | 1 todo (184)`

Score: 8/8 truths verified

**REQUIREMENTS COVERAGE section** (13-row table):

| Req | Status | Evidence |
| BODY-01 | ✅ | `Body.none()` in `src/body.ts`: `return toBody({ kind: "none" })` → no request body. `grep '"none"' src/body.ts` → present. tests/unit/body.test.ts BODY-01 test passes. |
| BODY-02 | ✅ | `Body.json(value)` in `src/body.ts`: stores `{ kind: "json", value }` opaquely; `serializeBody()` calls `JSON.stringify(value)` and returns `{ init: utf8bytes, contentType: "application/json" }`. TS6 fix: `Uint8Array<ArrayBuffer>` used for BodyInit compatibility. tests/unit/body.test.ts BODY-02 test passes. |
| BODY-03 | ✅ | `Body.text(value, contentType?)` in `src/body.ts`: defaults to `"text/plain; charset=utf-8"` when contentType omitted. `grep '"text/plain' src/body.ts` → present. tests/unit/body.test.ts BODY-03 test passes. |
| BODY-04 | ✅ | `Body.formUrlEncoded(entries)` in `src/body.ts`: `buildUrlSearchParams(entries)` — `URLSearchParams` with repeated-key support for arrays. `grep 'URLSearchParams' src/body.ts` → present. `grep 'buildUrlSearchParams' src/body.ts` → present. Content-type: `application/x-www-form-urlencoded`. tests/unit/body.test.ts BODY-04 test passes. |
| BODY-05 | ✅ | `Body.bytes(bytes, contentType?)` in `src/body.ts`: raw binary passthrough with caller-supplied content-type. `grep '"bytes"' src/body.ts` → present. tests/unit/body.test.ts BODY-05 test passes. |
| DEC-01 | ✅ | `Decode.none()` in `src/decode.ts`: reads exactly one chunk via `reader.read()`. If `done: true` → success (void). If `done: false` → `reader.cancel()` + return `{ kind: "unexpectedBody" }`. `grep 'unexpectedBody' src/decode.ts` → present. `grep 'reader.cancel' src/decode.ts` → present. tests/unit/decode.test.ts DEC-01 tests pass. |
| DEC-02 | ✅ | `Decode.discard()` in `src/decode.ts`: `response.body?.cancel().catch(() => {})` — no reader allocated, no byte buffering. Optional chaining handles null body. `grep 'body?.cancel' src/decode.ts` → present. tests/unit/decode.test.ts DEC-02 test passes. |
| DEC-03 | ✅ | `Decode.text()` in `src/decode.ts`: calls `readBytes(response)` then `new TextDecoder("utf-8").decode(bytes)`. Empty body → `""` (readBytes returns zero-length array). `grep 'Decode.text\|TextDecoder' src/decode.ts` → present. tests/unit/decode.test.ts DEC-03 tests pass. |
| DEC-04 | ✅ | `Decode.json()` in `src/decode.ts` (no schema): reads bytes, empty → `{ kind: "emptyBody" }`, parse via `JSON.parse()`, parse failure → `{ kind: "invalidJson", message }`. `grep 'emptyBody\|invalidJson' src/decode.ts` → both present. tests/unit/decode.test.ts DEC-04 tests pass. |
| DEC-05 | ✅ | `Decode.json(schema)` in `src/decode.ts`: passes parsed JSON to `schema.safeParse()`; on failure normalizes via `normalizeSchemaError()` duck-typing `.issues` array — `typeof error === "object" && "issues" in error && Array.isArray(...)`. NEVER `instanceof ZodError`. `grep 'schemaMismatch' src/decode.ts` → present. `grep 'instanceof' src/decode.ts` → 0. tests/unit/decode.test.ts DEC-05 tests pass. |
| DEC-06 | ✅ | `Decode.bytes()` in `src/decode.ts`: returns `await readBytes(response)` directly as `Uint8Array<ArrayBuffer>`. `grep '"bytes"' src/decode.ts` → present in Decode namespace. tests/unit/decode.test.ts DEC-06 test passes. |
| DEC-07 | ✅ | `Decode.optional(inner)` in `src/decode.ts`: reads all bytes first; zero bytes → `undefined`; non-zero → creates `new Response(bytes)` and delegates to `inner.fn(syntheticResponse)`. `grep 'optional' src/decode.ts` → present; `grep 'new Response(bytes)' src/decode.ts` → present. tests/unit/decode.test.ts DEC-07 tests pass. |
| DEC-08 | ✅ | `readBytes()` in `src/decode.ts`: `if (response.body === null) return new Uint8Array(0)` — null body fast path. For non-null bodies, empty ReadableStream (e.g., 200 + Content-Length: 0) produces zero-length Uint8Array naturally. `grep 'body === null\|body == null' src/decode.ts` → present. Both null-body (204/304/205) and empty-stream paths produce zero bytes before any decoder runs. tests/unit/decode.test.ts DEC-08 tests pass. |

**PHASE 4 TYPE ADDITIONS section:**

Phase 4 also extended src/types.ts with:
- `export declare class Body` — opaque class type token (private constructor, private `_kind`) — enables typed `Body` values without exposing internals
- `export type RequestError` — 5-variant discriminated union added to types.ts
- `SendResult<R>` extended from 4 to 5 variants: `{ kind: "requestError"; error: RequestError }` added
- These are documented because they affect the overall type surface and the TYPES-02 requirement

**DECISIONS COVERAGE section:**

| Decision | Status | Evidence |
| D-07 (body serialization deferred) | ✅ | `JSON.stringify` in `serializeBody()` (not in `Body.json()`); catch belongs in `send.ts`. `grep 'JSON.stringify' src/body.ts` → in serializeBody; `grep 'JSON.stringify' src/body.ts` NOT in Body.json factory. |
| D-03 (discard via cancel) | ✅ | `response.body?.cancel()` — no reader, no allocation. `grep 'body?.cancel' src/decode.ts` → present. |
| D-04 (none peeks exactly one chunk) | ✅ | `reader.read()` called once; `done` check; cancel on non-empty. |
| D-05 (optional via synthetic Response) | ✅ | `new Response(bytes)` forwarded to inner decoder; reads all bytes first. `grep 'new Response' src/decode.ts` → present. |
| D-10 (normalizeSchemaError duck-types) | ✅ | `typeof error === "object" && "issues" in error && Array.isArray(...)` pattern. `grep 'issues' src/decode.ts` → present. |
| D-11 (preview extracted) | ✅ | `src/preview.ts` created; `readBodyPreview` removed from send.ts. `grep -c 'async function readBodyPreview' src/send.ts` → 0. |

**KEY LINK VERIFICATION section:**

| From | To | Via | Status | Details |
| src/send.ts | src/body.ts | `import { serializeBody }` | ✓ WIRED | `grep 'import.*serializeBody' src/send.ts` → present; body serialized before fetchInit |
| src/send.ts | src/preview.ts | `import { readBodyPreview }` | ✓ WIRED | `grep 'import.*readBodyPreview' src/send.ts` → present |
| src/decode.ts readBytes | null-body fast path | `if (response.body === null) return new Uint8Array(0)` | ✓ WIRED | Critical for DEC-08; prevents hang on 204/304 |
| src/index.ts | src/body.ts, src/decode.ts | `export { Body } from "./body.js"; export { Decoder, Decode } from "./decode.js"` | ✓ WIRED | Public API surface complete; `grep 'export.*Body\|export.*Decode' src/index.ts` → both present |

**TEST RESULTS section:**
```
Test Files  13 passed (13)
     Tests  180 passed | 3 skipped | 1 todo (184)
  Start at  [time]
  Duration  ~3s
```

Key suites contributing to Phase 4 verification:
- tests/unit/body.test.ts — 13 tests (BODY-01..05), all pass
- tests/unit/decode.test.ts — 24 tests | 1 todo (DEC-01..08), all assertions pass
- tests/unit/types.test.ts — 13 tests including TYPES-09 (RequestError 5-variant), all pass

**ISSUES FOUND section:**

None. Phase 4 implementation is complete and correct. The `Body` type export collision (TS2300) was resolved by exporting only the value `Body` from index.ts (consumers get the type through inference); this is documented in 04-04-SUMMARY.md as an intentional decision, not a defect.

**SUMMARY section:**

Phase 4 Body Producers + Decoders is fully implemented and verified. The `Body` namespace provides 5 opaque producer factories with serialization deferred to send time. The `Decode` namespace provides 7 decoder factories including the critical `none()` peek-one-chunk strategy, `discard()` via direct cancel, and `json(schema)` with Zod-independent error normalization. `readBodyPreview()` was extracted to `src/preview.ts` with peek-read truncation detection. Body serialization errors are caught in `send.ts` and returned as `requestError.bodySerializationFailed`. Phase 4 also extended `SendResult<R>` to 5 variants by adding `requestError`.

**FOOTER:**
```
_Verified: [TODAY_ISO_DATE]_
_Verifier: gsd-verifier (agent)_
```
  </action>

  <verify>
    <automated>
      test -f .planning/phases/04-body-producers-decoders/04-VERIFICATION.md \
        &amp;&amp; grep -q 'status: passed' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md \
        &amp;&amp; grep -q 'DEC-05' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md \
        &amp;&amp; grep -q 'DEC-08' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md \
        &amp;&amp; grep -q 'normalizeSchemaError\|duck-typ' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md \
        &amp;&amp; echo "PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` exits 0
    - `grep 'status: passed' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` matches
    - `grep 'score: 13/13' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` matches
    - All 13 requirement IDs appear: BODY-01..05 and DEC-01..08
    - `grep 'normalizeSchemaError\|duck-typ' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` matches (DEC-05 Zod-free documented)
    - `grep 'body === null\|null.*body\|null-body' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` matches (DEC-08 evidence present)
    - `grep 'unexpectedBody' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` matches (DEC-01 evidence)
    - `grep 'requestError\|5th variant\|5-variant\|five variant' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` matches (SendResult extension noted)
    - `grep -c '✅' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md` returns ≥ 13
  </acceptance_criteria>

  <done>04-VERIFICATION.md exists with status: passed, all 13 Phase 4 requirements covered with specific grep evidence, DEC-05 Zod-free normalization documented, DEC-08 null-body normalization documented, and Phase 4's addition of the requestError variant noted.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md — mark 34 Phase 1-4 requirements Complete</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (read the full file before editing — verify current state of all 34 [ ] checkboxes and "Pending" entries in the traceability table)
    - .planning/phases/01-infrastructure-fixes/01-VERIFICATION.md (must exist from Plan 09-01 before editing)
    - .planning/phases/02-core-types-request-model/02-VERIFICATION.md (must exist from Plan 09-02)
    - .planning/phases/03-transport-send/03-VERIFICATION.md (must exist from Plan 09-03)
    - .planning/phases/04-body-producers-decoders/04-VERIFICATION.md (must exist from Task 1 above)
  </read_first>

  <action>
Edit `.planning/REQUIREMENTS.md` to make exactly two categories of changes:

**CHANGE CATEGORY 1: Requirement checkboxes in the "v1 Requirements" section**

Change `[ ]` → `[x]` for these 34 lines (leave all other lines untouched):

Phase 1 (Infrastructure Fixes section):
- `- [ ] **INFRA-01**:` → `- [x] **INFRA-01**:`
- `- [ ] **INFRA-02**:` → `- [x] **INFRA-02**:`
- `- [ ] **INFRA-03**:` → `- [x] **INFRA-03**:`

Phase 2 (Core Types section and Request Model section):
- `- [ ] **TYPES-01**:` → `- [x] **TYPES-01**:`
- `- [ ] **TYPES-02**:` → `- [x] **TYPES-02**:`
- `- [ ] **TYPES-03**:` → `- [x] **TYPES-03**:`
- `- [ ] **TYPES-04**:` → `- [x] **TYPES-04**:`
- `- [ ] **TYPES-05**:` → `- [x] **TYPES-05**:`
- `- [ ] **TYPES-06**:` → `- [x] **TYPES-06**:`
- `- [ ] **TYPES-07**:` → `- [x] **TYPES-07**:`
- `- [ ] **TYPES-08**:` → `- [x] **TYPES-08**:`
- `- [ ] **REQ-01**:` → `- [x] **REQ-01**:`
- `- [ ] **REQ-02**:` → `- [x] **REQ-02**:`
- `- [ ] **REQ-03**:` → `- [x] **REQ-03**:`
- `- [ ] **REQ-04**:` → `- [x] **REQ-04**:`

Phase 3 (Transport & Send section):
- `- [ ] **SEND-01**:` → `- [x] **SEND-01**:`
- `- [ ] **SEND-02**:` → `- [x] **SEND-02**:`
- `- [ ] **SEND-03**:` → `- [x] **SEND-03**:`
- `- [ ] **SEND-04**:` → `- [x] **SEND-04**:`
- `- [ ] **SEND-05**:` → `- [x] **SEND-05**:`
- `- [ ] **SEND-06**:` → `- [x] **SEND-06**:`

Phase 4 (Body Producers section and Body Decoders section):
- `- [ ] **BODY-01**:` → `- [x] **BODY-01**:`
- `- [ ] **BODY-02**:` → `- [x] **BODY-02**:`
- `- [ ] **BODY-03**:` → `- [x] **BODY-03**:`
- `- [ ] **BODY-04**:` → `- [x] **BODY-04**:`
- `- [ ] **BODY-05**:` → `- [x] **BODY-05**:`
- `- [ ] **DEC-01**:` → `- [x] **DEC-01**:`
- `- [ ] **DEC-02**:` → `- [x] **DEC-02**:`
- `- [ ] **DEC-03**:` → `- [x] **DEC-03**:`
- `- [ ] **DEC-04**:` → `- [x] **DEC-04**:`
- `- [ ] **DEC-05**:` → `- [x] **DEC-05**:`
- `- [ ] **DEC-06**:` → `- [x] **DEC-06**:`
- `- [ ] **DEC-07**:` → `- [x] **DEC-07**:`
- `- [ ] **DEC-08**:` → `- [x] **DEC-08**:`

DO NOT change: ADR-01..07 checkboxes (those remain [ ] — addressed in Phase 10), or any currently [x] entries.

**CHANGE CATEGORY 2: Traceability table in the "## Traceability" section**

Change `| Pending |` → `| Complete |` for these 34 rows. Match by requirement ID in the first column. Only change rows whose first column is one of the 34 listed above.

Rows to update (format: `| INFRA-01 | Phase 1 | Pending |` → `| INFRA-01 | Phase 1 | Complete |`):
- INFRA-01, INFRA-02, INFRA-03
- TYPES-01, TYPES-02, TYPES-03, TYPES-04, TYPES-05, TYPES-06, TYPES-07, TYPES-08
- REQ-01, REQ-02, REQ-03, REQ-04
- SEND-01, SEND-02, SEND-03, SEND-04, SEND-05, SEND-06
- BODY-01, BODY-02, BODY-03, BODY-04, BODY-05
- DEC-01, DEC-02, DEC-03, DEC-04, DEC-05, DEC-06, DEC-07, DEC-08

DO NOT change: ADR-01..07 rows (remain Pending), RESP-01..04 rows (already Complete), PREV-01..03 rows (already Complete), MATCH-01..03 rows (already Complete), DOC-01..03 rows (already Complete).

**Also update the "Last updated" metadata line at the bottom of REQUIREMENTS.md:**
Change: `*Last updated: 2026-05-04 after initial research synthesis*`
To: `*Last updated: [TODAY_DATE] — Phase 9: Phases 1–4 requirements marked Complete*`
  </action>

  <verify>
    <automated>
      # Count [x] checkboxes for the 34 Phase 1-4 requirements
      grep -c '\[x\].*INFRA-\|INFRA.*\[x\]' .planning/REQUIREMENTS.md
      grep -c '\[x\].*TYPES-\|\[x\].*REQ-[0-9]' .planning/REQUIREMENTS.md
      grep -c '\[x\].*SEND-\|\[x\].*BODY-\|\[x\].*DEC-' .planning/REQUIREMENTS.md
      # Count Complete entries for Phase 1-4 in traceability table
      grep -c '| INFRA-.*| Complete |' .planning/REQUIREMENTS.md
      grep -c '| SEND-.*| Complete |' .planning/REQUIREMENTS.md
      grep -c '| DEC-.*| Complete |' .planning/REQUIREMENTS.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '\[x\] \*\*INFRA-' .planning/REQUIREMENTS.md` returns 3 (INFRA-01, INFRA-02, INFRA-03)
    - `grep -c '\[x\] \*\*TYPES-' .planning/REQUIREMENTS.md` returns 8 (TYPES-01 through TYPES-08)
    - `grep -c '\[x\] \*\*REQ-' .planning/REQUIREMENTS.md` returns 4 (REQ-01 through REQ-04)
    - `grep -c '\[x\] \*\*SEND-' .planning/REQUIREMENTS.md` returns 6 (SEND-01 through SEND-06)
    - `grep -c '\[x\] \*\*BODY-' .planning/REQUIREMENTS.md` returns 5 (BODY-01 through BODY-05)
    - `grep -c '\[x\] \*\*DEC-' .planning/REQUIREMENTS.md` returns 8 (DEC-01 through DEC-08)
    - `grep '| ADR-01 ' .planning/REQUIREMENTS.md | grep 'Pending'` matches (ADR rows unchanged)
    - `grep '| RESP-01 ' .planning/REQUIREMENTS.md | grep 'Complete'` matches (existing Complete rows preserved)
    - `grep 'INFRA-01.*Complete\|Complete.*INFRA-01' .planning/REQUIREMENTS.md` matches (traceability table updated)
    - `grep 'DEC-08.*Complete\|Complete.*DEC-08' .planning/REQUIREMENTS.md` matches (last row updated)
    - `grep '[ ].*ADR-' .planning/REQUIREMENTS.md | grep -v '\[x\]'` returns lines (ADR checkboxes still unchecked)
  </acceptance_criteria>

  <done>REQUIREMENTS.md has all 34 Phase 1-4 requirement checkboxes changed from [ ] to [x], all 34 traceability table rows changed from Pending to Complete, ADR/RESP/PREV/MATCH/DOC rows unchanged, and the last-updated metadata line updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→executor | Executor reads SUMMARY files, writes VERIFICATION.md, then edits REQUIREMENTS.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | REQUIREMENTS.md edits | mitigate | Read file before editing; verify with grep counts after; only modify the 34 specified rows |
| T-09-04-02 | Tampering | 04-VERIFICATION.md content | accept | Documentation file; no executable code |
| T-09-04-03 | Information Disclosure | SUMMARY files | accept | Internal planning docs; no PII or secrets |
</threat_model>

<verification>
```bash
# VERIFICATION.md exists and passes
test -f .planning/phases/04-body-producers-decoders/04-VERIFICATION.md
grep 'status: passed' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md
grep 'score: 13/13' .planning/phases/04-body-producers-decoders/04-VERIFICATION.md

# REQUIREMENTS.md checkbox counts
grep -c '\[x\] \*\*INFRA-' .planning/REQUIREMENTS.md   # expect 3
grep -c '\[x\] \*\*TYPES-' .planning/REQUIREMENTS.md   # expect 8
grep -c '\[x\] \*\*REQ-'   .planning/REQUIREMENTS.md   # expect 4
grep -c '\[x\] \*\*SEND-'  .planning/REQUIREMENTS.md   # expect 6
grep -c '\[x\] \*\*BODY-'  .planning/REQUIREMENTS.md   # expect 5
grep -c '\[x\] \*\*DEC-'   .planning/REQUIREMENTS.md   # expect 8

# Traceability table updated
grep 'INFRA-01.*Complete' .planning/REQUIREMENTS.md
grep 'DEC-08.*Complete'   .planning/REQUIREMENTS.md

# Preserved rows unchanged
grep '| ADR-01 ' .planning/REQUIREMENTS.md | grep 'Pending'
grep '| RESP-01 ' .planning/REQUIREMENTS.md | grep 'Complete'
```
</verification>

<success_criteria>
- 04-VERIFICATION.md exists with `status: passed` and all 13 Phase 4 requirements covered
- REQUIREMENTS.md: exactly 34 new [x] checkboxes (INFRA/TYPES/REQ/SEND/BODY/DEC groups)
- REQUIREMENTS.md: traceability table rows for all 34 Phase 1-4 requirements show Complete
- ADR-01..07 rows in traceability table remain Pending (not Phase 9's scope)
- Previously-Complete rows (RESP, PREV, MATCH, DOC) remain Complete and unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-retroactive-phase-verifications/09-04-SUMMARY.md`
</output>
